# Core AI / LangChain (modern LCEL API — LLMChain/SequentialChain removed in 1.0+)
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
llama-cpp-python>=0.2.77          # CPU-only GGUF inference (SmolLM)
openai-whisper>=20231117          # Whisper STT (fallback if whisper.cpp unavailable)

# Security
cryptography>=42.0.0              # Fernet AES-128-CBC
python-jose[cryptography]>=3.3.0  # JWT tokens

# Database
redis>=5.0.0
aiosqlite>=0.20.0

# Audio
numpy>=1.24.0
soundfile>=0.12.1

# HTTP / API
fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0
websockets>=12.0

# Fine-tuning
torch>=2.2.0                      # CPU-only, no CUDA needed
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.30.0
peft>=0.10.0                      # LoRA fine-tuning (memory efficient)
trl>=0.8.0                        # SFTTrainer

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
loguru>=0.7.0
tqdm>=4.66.0
sentencepiece>=0.2.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
